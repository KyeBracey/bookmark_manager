<h1>Links</h1>
<ul id="links">
  <% @links.each do |link| %>
      <li> 
        <a href="<%= link.url %>" target="_blank"><%= link.title %> </a> (<%= link.tags %>) 
      </li>
  <% end %>
</ul>

<a href="/links/new">Create Link</a>